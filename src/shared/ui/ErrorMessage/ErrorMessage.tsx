import { Typography } from "@mui/material";

export const ErrorMessage = () => (
  <Typography variant="h6" textAlign="center">
    An error occurred on the server. Please try again later or try to reload the
    page
  </Typography>
);
